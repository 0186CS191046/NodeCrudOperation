// // controllers/employeeController.js
// const db = require('../models/employee');

// // Create a new employee
// async function createEmployee(req, res) {
//   try {
//     const employee = await db.Employee.create(req.body);
//     res.json(employee);
//   } catch (error) {
//     console.error(error);
//     res.status(500).json({ error: 'Unable to create the employee.' });
//   }
// }

// createEmployee()
